export function Eyebrow({ children }: { children: React.ReactNode }) {
  return (
    <p className="font-body text-xs font-semibold uppercase tracking-[0.2em] text-oxblood">
      {children}
    </p>
  );
}
